# 录像模块参数
video_resolution: "1080p"
video_duration: 180        # 录像时长(秒)；scenario 里 task.duration 可覆盖
video_min_size_kb: 100
video_capture_timeout: 15
video_ftp_download: true   # 录像后是否 FTP 校验大小+下载；false=纯录像（完全不依赖 FTP，场景层 override 覆盖）
