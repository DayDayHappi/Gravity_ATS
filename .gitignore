# ============================================================
# 跟踪范围：ATS/（源码）、tools/（内置 ffmpeg 二进制）、
#           docs/（文档知识库）、.claude/agents/（Document Agent 规则）
# 运行产物与本地敏感配置忽略（不进 git）
# ============================================================

# 运行产物
logs/
reports/

# 实测样本（开发参考，保留本地）
res/

# Claude 配置与计划文件（仅放行 Document Agent 规则，settings.local.json 等继续忽略）
.claude/*
!.claude/agents/
!.claude/agents/document-agent.md

# 根目录杂项
migrate.sh
all_source.txt
auto.crt
auto.key

# Python 字节码
__pycache__/
*.py[cod]
*.pyo

# 编辑器 / 临时文件
*.swp
*~
.DS_Store
