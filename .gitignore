# ============================================================
# 跟踪范围：ATS/（源码）、docs/（文档知识库）、.claude/agents/（Document Agent 规则）
# 运行产物与本地敏感配置忽略（不进 git）
# ============================================================

# tools/（内置 ffmpeg 二进制 + 工具脚本）：本地构建产物/工具，不跟踪其变动
tools/

# 运行产物
logs/
reports/
downloads/

# 实测样本（开发参考，保留本地）
res/

# Claude 配置与计划文件（仅放行 Document Agent 规则，settings.local.json 等继续忽略）
.claude/*
!.claude/agents/
!.claude/agents/document-agent.md

# 根目录杂项
migrate.sh
all_source.txt
auto.crt
auto.key

# Python 字节码
__pycache__/
*.py[cod]
*.pyo

# 编辑器 / 临时文件
*.swp
*~
.DS_Store

# tests/（单元测试源码，属 Code Agent 交付物，跟踪进 git；与 .claude/agents/ 同类放行）
# 注：曾有 "/tests/" 忽略规则致测试源码未入 git（ADR-016 QA-GAP-02），已移除。
