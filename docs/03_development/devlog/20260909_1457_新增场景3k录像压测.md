# 新增场景 stress_traverse_photo_mode_3k（3k 录像压测）

## 日期
2026-09-09

## 变更来源
`docs/05_handoff/next_step.md` P1「新增场景 3k 录像压测（待实施）」，需求文档 [req_stress_traverse_photo_mode_3k.md](../03_development/archive/req_stress_traverse_photo_mode_3k.md)。

## 做了什么
复制 `ATS/config/scenarios/stress_traverse_photo_mode.yaml` 为 `stress_traverse_photo_mode_3k.yaml`，唯一差异：video task 加一行 `override.video_resolution: "3k"`，其余（preview/prepare/loop.count=200/photo/video_integrity/rtmp duration=66/cleanup）与源文件逐字一致。

- 纯配置，不改源码，不碰 `config/modules/video.yaml` 全局默认（保持 `"1080p"`，3k 仅场景级 override 生效）。
- `scenario.name` 与文件名一致：`stress_traverse_photo_mode_3k`（ASCII 安全，`--scenario` 可直接引用）。

## 改了哪些文件
- `ATS/config/scenarios/stress_traverse_photo_mode_3k.yaml`（新建）

## 验证结果
1. `python3 -m ATS.main --list-scenarios` → 识别 `stress_traverse_photo_mode_3k` PASS。
2. yaml 校验：`scenario.name` 正确、video task `override.video_resolution == "3k"`、源文件 video task 无 override、`config/modules/video.yaml` 仍为 `1080p` → 全 PASS。
3. 未跑真机（待 P0 批次统一真机验证）。

## 边界确认（需求文档 §7）
- 不改源码，现有 override 机制已够用。
- 若真机发现 `cam_set video 3k` 后 `video_integrity.yaml` 的 `expected_fps`/`expected_gop_size`（现 30/30）或 `video_min_size_kb` 阈值与 3k 落盘（2268×3024 竖屏，文件更大/GOP 结构可能不同）不符，按需求文档要求**停下产出 Document Agent Request**，不自行调参。本 devlog 记录时未真机、未触发此边界。

## 文档影响（报告给 Document Agent，Code Agent 不改）
- `next_step.md` P1「新增场景 3k 录像压测（待实施）」→ 已实施，状态改为「待真机」。
