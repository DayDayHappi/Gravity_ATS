# video 录像前恢复 cam_set

## 日期
2026-09-07

## 变更来源
Document Agent Request：testvideo 分支已做过一次（commit dcf19ff），但未合入 new_arch。本分支 new_arch 的 video.py 与 testvideo 已大幅分叉，只挑「恢复 cam_set」这一小段。

## 做了什么
删除录像前的临时跳过逻辑（`if False:` 包裹 + 3 行 `TODO-TEMP-DISABLE-CAM_SET` 注释），把 `cam_set video {resolution}` 还原为顶格同步调用。

## 改了哪些文件
- `ATS/modules/video.py`（仅 L56-59 区域：删 `if False:` 包裹与 TODO 注释，cam_set 还原顶格；resolution 变量、docstring 均保持不变）

## 如何验证
1. `python3 -m py_compile ATS/modules/video.py` → PASS。
2. `grep -n "if False\|TODO-TEMP-DISABLE-CAM_SET" ATS/modules/video.py` → 无残留标记。

## 已知遗留
- 默认分辨率 3k（TODO-CONFIRM：模块默认 `ATS/config/modules/video.yaml` 的 `video_resolution: "1080p"` 还是场景 override，或代码 fallback）待用户拍板，本次未动。
- EIS 参数（历史命令 `cam_set video 3k 2` 尾随 `2`）本次未处理，只落档位字符串，不碰 EIS。

## 文档影响（报告给 Document Agent，Code Agent 不改）
- `docs/05_handoff/next_step.md` L17 / `current_status.md` L33、L39：「录像前暂时取消 cam_set（待恢复）」→ 已恢复，待 Document Agent 更新为已完成。
- `docs/03_development/devlog/20260817_0203...`「板子分辨率只支持 4k/1080p 两档」已过时（固件已支持 3k，真机日志佐证），待 Document Agent 标注过时。
