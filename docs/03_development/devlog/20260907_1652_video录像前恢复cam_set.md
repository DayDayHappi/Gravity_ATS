# video 录像前恢复 cam_set 发送

## 日期

2026-09-07

## Task

恢复 `ATS/modules/video.py` 录像前临时禁用的 `cam_set video <resolution>` 调用（还原 TODO-TEMP-DISABLE-CAM_SET 跳过逻辑），并同步修正模块 docstring。

## 背景

2026-08-27 起，录像前临时禁用了 `cam_set video <resolution>`，改用 `if False:` 包住原调用段并加 `TODO-TEMP-DISABLE-CAM_SET` 标记。现用户口述要求恢复，还原「切分辨率 → 录像」完整流程。

## 变更

- `run()`（FTP 下载模式）：删除 `TODO-TEMP-DISABLE-CAM_SET` 三行注释与 `if False:` 包裹层，还原 `cam_set video {resolution}` 为顶格同步调用（约 L71–73）。
- `_run_no_ftp()`（纯录像模式）：删除 `TODO-TEMP-DISABLE-CAM_SET` 两行注释与 `if False:` 包裹层，还原 `cam_set video {resolution}` 调用（约 L164–166）。
- 模块 docstring「流程（纯录像模式）」补列 `cam_set video 1080p` 第 1 步（与 FTP 模式 docstring 一致）。

## Files

- ATS/modules/video.py

## Reason

用户口述要求恢复录像前切分辨率动作；临时禁用标记已无存在必要，需彻底移除。

## 验证

- `grep -rn "TODO-TEMP-DISABLE-CAM_SET\|if False:" ATS/modules/video.py` → 空（exit=1，无残留）PASS
- `grep -n "cam_set video" ATS/modules/video.py` → 命中两处实际调用（L71 run()、L164 _run_no_ftp()；L14/L20 为 docstring 文本）PASS
- `python3 -m py_compile ATS/modules/video.py` → PASS
- `python3 -m ATS.main --list-modules` → 仍识别 `video`、`video_integrity` 模块，无 import 破坏 PASS
- 真机（可选）→ NOT RUN（环境未就绪）

## Known Limitations

无。真机录像前串口 `cam_set video 1080p` 发送行为未实测，待环境就绪后按需验证。

## Documentation Impact

Document Agent review recommended（handoff `next_step.md` / `current_status.md` 中「录像前暂时取消 cam_set / 待恢复」相关条目需 Document Agent 后续清理，Code Agent 不代做）。
