# video 录像前暂时取消 cam_set（临时禁用，后续恢复）

## 日期
2026-08-27

## 变更来源
`docs/05_handoff/next_step.md` 顶部 🔴 紧急待办（用户口述，Document Agent 交接，commit a36abc4）。

## 做了什么
临时禁用录像前的 `cam_set video <resolution>` 调用：改为直接 `dfs_video_start`。

- 用 `if False:` 包住原 `cam_set` 段（含其失败 return），显式跳过而非删除，保留可回退的完整原逻辑。
- 加显式标记注释 `TODO-TEMP-DISABLE-CAM_SET`，说明「录像前暂不切分辨率，后续恢复 cam_set video」。
- `resolution` 变量保留未删（`logger.step` / `logger.info` 仍在用）。
- 恢复方法：删掉 `if False:` 跳过逻辑（连同标记注释），把 `cam_set` 段缩进还原为顶格即可。

## 改了哪些文件
- `ATS/modules/video.py`（仅 L56-59 区域，临时禁用，无接口/行为面之外的改动）

## 如何验证
1. `python3 -m py_compile ATS/modules/video.py` → PASS。
2. `from ATS.modules import video` → PASS。
3. `inspect.getsource(run)` 确认含 `TODO-TEMP-DISABLE-CAM_SET` 标记 + `if False:` 跳过逻辑 + `resolution` 变量保留 → PASS。

## 已知遗留
- 本改动为**临时禁用**，后续需恢复 `cam_set video`（见 TODO 标记），恢复后本 devlog 无需回改（历史事实快照）。
- 尚未真机验证（与 current_status P0 一致）。

## 文档影响（报告给 Document Agent，Code Agent 不改）
next_step.md 顶部该 🔴 待办本身已完成，应由 Document Agent 更新状态（或在本条标注「已临时实施，待后续恢复」）。
