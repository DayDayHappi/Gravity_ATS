# stress 场景循环次数 3 改 10

## 日期
2026-08-24

## 变更来源
人工手动调整（未走 Code Agent 流程）。

## 做了什么
`ATS/config/scenarios/stress.yaml` 的 `loop.count` 由 3 改为 10（压测整轮循环从 3 次增至 10 次）。

## 改了哪些文件
- `ATS/config/scenarios/stress.yaml` — `loop.count: 3 → 10`

## 如何验证
未真机验证（本轮改动与 ADR-008/009 同批，均待真机）。

## 遗留（待 Code Agent）
第 1 行注释仍写「整轮循环 3 次」，与 `count: 10` 不符，需同步为「10 次」。
