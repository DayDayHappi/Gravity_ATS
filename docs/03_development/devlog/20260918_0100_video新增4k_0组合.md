# 20260918 video 新增 4k_0 组合

## 任务（Task）

录像 size 命令表新增组合 `cam_set video 4k 0`（key `4k_0`），只加定义、不接场景，预期 size 待真机 ffprobe 校准。

## 改动（Changed）

`ATS/drivers/video_commands.py` 的 `VIDEO_PROFILES` 映射**最前**（`sd1080p_0` 之前）新增：

```python
"4k_0": VideoProfile("4k_0", "cam_set video 4k 0", 0, 0, "待校准"),
```

- key 命名 `4k_0`，与 `3k_2` / `480p_1` 同构（`4k` + 下标参数 `0`）。
- 占位值 `width=0, height=0, orientation="待校准"`：预期 size 未知，`0x0` 仅表示未校准（`VideoProfile` 三字段必填，且 video.py 只用 width/height 做 report 展示、不参与判据计算，0 占位最诚实）。
- 附 TODO-CONFIRM 注释：待真机 ffprobe 校准后回填。

## 文件（Files）

- 修改：`ATS/drivers/video_commands.py`

## 原因（Reason）

用户新增需求：录像新增 size 组合 `cam_set video 4k 0`（历史上 v1p2 曾删除 4k，现固件新增功能重新支持，用户确认合法）。只加定义，暂不接场景，不动 `BLOCKED_VIDEO_PROFILES` / `video.py` / Runner / Scenario。

## 验证（Verification）

- `resolve_video_profile("4k_0")` 返回 `VideoProfile(key='4k_0', command='cam_set video 4k 0', width=0, height=0, orientation='待校准')`，不抛 `VideoProfileError`。
- `VIDEO_PROFILES` 总数 12 → 13，`4k_0` 位于映射最前；现有 12 个组合 `resolve_video_profile` 行为不变（纯增量）。
- `py_compile` + `--list-modules` / `--list-scenarios` / `--dry-run` 无 import 破坏。

## 已知限制（Known limitation）

- 预期 size 未知，占位 `0, 0, "待校准"`；真机 `cam_set video 4k 0` 后用 ffprobe 实测回填 width/height/orientation，关闭 TODO-CONFIRM。
- 未接任何 scenario，`4k_0` 不会被执行；`video_size_traverse.yaml` 头注释「11 个组合」仍准确（该场景未接入 4k_0）。
