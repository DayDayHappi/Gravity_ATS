Place the Windows x64 runtime files here before building:
- ffmpeg.exe
- ffprobe.exe
- ffplay.exe

Use scripts/prepare_runtime.ps1 to copy an installed FFmpeg build. Retain the
corresponding license and source-offer notices required by that distribution.
